import { ArrayNumberToDatePipe } from './array-number-to-date.pipe';

describe('ArrayNumberToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayNumberToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
